# HC1 HEALTH — 2026-09-06 (Games repo, madebymatt-play.uk)

Token `mbm-two-domain-healthcheck-2026-09-06-HC1` · verdict **HC1_PARTIAL**. Estate
ledger and map: `Lessons/docs/HC1_HEALTH_2026-09-06.md`, `Lessons/docs/ESTATE_MAP_2026-09-06.md`.
Measured at main `ec5ee7bb`; publication pins Site `ff764b65`, Lessons `78ff8fc9`.

## Served proof at HEAD
`Standalone games website` run 34038125168: build ✓ (69 payloads, 0 missing initial-load
refs, `games.json` byte-identical to the shelf, CNAME `madebymatt-play.uk`), Pages guard
✓ (build type Actions, cname apex/www, HTTPS 200 on `/data/domain-catalogue.json`),
deploy ✓ (job 101499958375), `verify-published` ✓ (phone + desktop journeys, previews,
retained payloads; job 101499986608).

## Measured on the rebuilt play tree (465 files, 81 html)
- 62 games + 6 classroom activities + 1 staff activity; home served at `/`, `/games/`,
  `/Games/`, `/main/`, `/for/pupils/`, `/Lessons/`; `/privacy/`, `/stats/`, `/game-saves/`
  (import leg). canonical/og:url on `www.madebymatt-play.uk` (86). Sitemap 70 own locs.
- Cross-origin: 0 loads from `madebymatt.uk`; 4 home variants link back to education.
  0 third-party subresources. Storage: per-game keys only; 1 service worker
  (`micro-tinkerer/sw.js`, game-scoped; precache not verified from here).
- Runtime (headless, 390×844, no GPU): 81/81 load with 0 console errors, 0 failed
  requests. First paint median 1.06 s, p90 5.9 s; 19 routes load > 3 s (`Prism` 17 s,
  `Slipstream` 15 s, `Orbital` 14.8 s, `Neon_Garden` 14.4 s, `Neon_Siege` 12.8 s).
- Bytes: 20.3 MB raw / 6.3 MB gzip across the 69; 16 over 500 KB raw (`/titanforge/`
  1.6 MB + 589 KB vendor, `/apexkick/` 971 KB, `/emberwild/` 967 KB, `Wrecking_Crew`
  846 KB, `/voxel/` 719 KB …). Measured, nothing enforced.
- Idle repaint: 18/69 paint zero frames in 600 ms idle after boot; busiest
  `/offbrand/` 148, `Charcoal` 144, `/apexgolf/` 72, `Wrecking_Crew` 70.
- Zoom blocked (`user-scalable=no`): `/apexrally/`, `/fracture/`, `/neonsync/`,
  `/neonmeridian/`, `/neonbreach/`, `Lessons/Games/Orbital.html`,
  `medevac/MedevacFrontier_v1.html` — source lives in Site/Lessons, not here.
- Duplicate ids in the DOM: 0. Way-out by Tab within 50 tabs not reached on 10 routes
  (splash gating; the Site `inline-exit-verify` gate, green 2026-09-06, is authoritative).
- Old URLs: all 62 shelf routes + 7 activities serve; the three legacy aliases
  (`/Lessons/Games/Trail_Runner.html`, `Voxel_Frontier.html`, `5_6 Local Choice/Trekkers…(2)`)
  do not exist here by design — the education stubs link to their play counterparts.

## Owned here
Nothing to correct. Future lesson-side changes need a reviewed `play-publication.json`
pin bump (currently Lessons `78ff8fc9`, one verifier-only commit behind Lessons main).
Rollback: main `ec5ee7bb2724dc0219a53b7e60508b42bbd2a051`.
