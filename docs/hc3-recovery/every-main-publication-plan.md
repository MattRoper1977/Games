Games #70 merged its documentation checkpoint as 6d7b1b3ceb07b144d60845be4a4bf46e2967e166, but the publication workflow's main push path filter excludes documentation; there is no publication run at that main SHA. HC3 exact-main readback cannot reuse an older source as proof. Make every main commit produce its own source-bound publication receipt, retaining all existing build, destination, save-boundary, live-byte and browser gates. Source game pins and payloads do not change.

Rollback before implementation: Games 6d7b1b3ceb07b144d60845be4a4bf46e2967e166; current published game release9237d177b1f26b8ea0d9b103fe141560f319ad02. No open Games PR owns the workflow (measured). Add a small control reading actual YAML: main including docs must publish; one planted path filter must fail; restored must pass. Keep PR publishing prohibited. Full applicable checks, expected-head merge, then exact-source build/deploy and69-route live proof required. No branch deletion or manual pin bump.
